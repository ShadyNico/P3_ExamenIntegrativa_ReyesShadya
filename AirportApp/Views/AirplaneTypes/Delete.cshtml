@model AirplaneType
@{ ViewData["Title"] = "Eliminar tipo"; }
<h1>Eliminar tipo de avión</h1><p>¿Eliminar <strong>@Model.Identifier</strong>?</p><form asp-route-id="@Model.TypeId" method="post"><button class="btn btn-danger">Eliminar</button> <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a></form>
